# 更新日志

所有重要的项目变更都会记录在此文件中。

格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.0.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [Unreleased]

### 安全
- 收紧 TMDB API 路由和远程图片 URL：只接受明确的 TMDB 路由语法，图片地址必须使用 HTTPS 默认端口、可信主机且不含认证信息、片段或控制字符。
- 本地监控（含独立目录新增、更新与启动）、指纹读取、刮削源文件、重命名预览和字幕扫描统一限制在 `MHTI_ALLOWED_MEDIA_ROOTS` 内；监控和字幕扫描额外拒绝普通文件冒充目录，避免通过接口探测或读取非媒体路径。
- 路径验证在解析符号链接或探测文件类型前先执行无文件系统访问的根目录边界检查，范围外的存在与不存在路径返回一致拒绝结果。
- 所有外部值写入服务日志前转换为有界单行文本，前端不再把 WebSocket 服务端载荷直接写入浏览器控制台，防止伪造日志记录。
- 增加 SSRF、路径边界和日志注入回归测试；保留全部 CodeQL 查询，并为已完成双重边界校验的路径返回值声明可审计的屏障模型，消除扫描发现的冗余文件系统数据流。

### 计划中
- 多语言支持 (i18n)
- 更多媒体源支持

---

## [2.1.6] - 2026-09-21

本版本集中收敛文件选择、本地与 115 存储、任务终态、Watcher/调度器恢复以及认证会话中的并发和失败边界。

### 文件选择与存储安全
- 文件扫描和任务向导现在会使用实际勾选的文件与目录，正确保留 115 locator、provider、分页与已刮削过滤状态。
- 海报、背景图、缩略图、NFO、字幕及覆盖选项会写入任务并在后端生效；预览与实际任务使用一致的递归视频扫描。
- 后端强制校验本地与 115 的存储能力组合：不再将 115 虚拟路径当本地路径，拦截不支持的输出、元数据目录和链接模式，并严格执行“允许下载到本地”许可。
- 本地与 115 输出先完成元数据再发布媒体；115 移动改名失败可回滚，复制后精确识别新副本 ID，避免误改同名旧文件或产生失效链接。

### 任务、Watcher 与调度可靠性
- 媒体已成功发布后，日志、字幕、审计记录、通知失败或服务关闭不再将任务反向覆盖为失败或已取消。
- AI 无匹配重试将新任务、旧任务替换和历史状态放入同一事务，防止双击或并发请求创建重复任务。
- 115 监控增加初始基线、事件范围、任意目录深度、完整分页、file_id/parent_id 持久保留和有界去重；创建任务失败的文件保留等待重试。
- Watcher 的创建、更新、删除和全局配置切换会串行化运行时变更，失败时回复数据库与原策略；策略停止不完整时拒绝写入新配置。
- 定时任务增加 Cron 校验、原子领取、有限退避重试和重启恢复；运行期间修改 Cron 或禁用时使用数据库最新配置，已禁用的中断任务不再重新排队。

### 认证、WebSocket 与运行时
- 密码修改与其他会话撤销在同一数据库事务内完成，被撤销和超出上限的会话立即关闭 WebSocket；并发登录不再突破会话上限。
- 管理员注册使用数据库单例约束；修改用户名后现有会话按稳定会话身份继续有效，不再被 WebSocket 周期校验强制退出。
- WebSocket 发送按连接串行化，增加发送超时、并行广播、订阅上限、旧连接回调隔离及会话周期复核；容器、日志和 API 统一使用同一个连接管理器。
- 日志写入增加有界缓冲、超时、失败批次恢复和关闭排空；应用关闭流程分阶段限时且故障隔离。
- 前端接通运行时 `API_BASE_URL`，刷新令牌和 WebSocket 跟随同一 API 主机及路径前缀；移除无法携带 Bearer Token 的未使用 SSE 客户端。

### 发布与升级
- GitHub Release 不再生成 AMD64/ARM64 离线 Docker `tar.gz` 大附件；多架构镜像继续发布到 GHCR。
- PR #57 至 #66 的后端测试、前端类型检查/Lint/单元测试/构建、依赖审计、Docker 构建及容器健康检查均由 GitHub Actions 执行；未在本机构建或运行测试。
- 保留现有配置、数据库、任务、历史记录和 115 登录状态，无需清空数据或手动迁移。

---

## [2.1.5] - 2026-09-18

本版本加强日志持久化和 WebSocket 生命周期管理，并建立可持续执行的前后端质量门禁。

### 日志与 WebSocket 可靠性
- 数据库日志批量写入异常不再被服务层吞掉；处理器会按原顺序恢复失败批次，并在并发写入和应用关闭时安全排空缓冲区。
- 应用关闭会等待日志处理器完成有限次数的最终重试，避免数据库连接先关闭导致尾部日志丢失。
- 移除未使用的日志缓冲器、WebSocket 用户动作通道和前端重复状态缓存，减少两套状态来源产生偏差的风险。
- 前端 WebSocket 将高频消息合并交付，取消消息立即送达，断开时清理待处理定时器和缓存；服务端超时监控改为单一任务读取最新活动时间。
- 新增日志并发、失败重试、关闭排空和前端 WebSocket 生命周期测试。

### 质量门禁与维护
- 新增前端 ESLint 门禁并清理无效变量，修复响应式取值在未知布局分支可能不返回值的问题。
- 对本轮可靠性模块启用增量 mypy 严格检查，并固定 pytest、ruff、pip-audit、mypy 等 CI 工具版本。
- 根据 GitHub Actions 的 78% 覆盖率基线设置 77% 防回退门槛。
- GitHub Actions 继续使用 Node.js 24，并升级 setup-node、upload-artifact 和 download-artifact，消除旧 Action 运行时告警。

### 验证与升级注意
- PR #55 和 #56 均仅通过 GitHub Actions 验证后端测试、前端类型检查、静态检查、单元测试及 Docker 容器健康状态；未在本机执行构建或测试。
- 保留现有配置、数据库、任务和历史记录，无需清空数据或手动迁移。

---

## [2.1.4] - 2026-09-17

本版本统一自动与手动匹配的输出流程，修复任务状态和日志可靠性问题，并完成冲突处理界面的拆分与旧架构清理。

### 刮削流程与可靠性
- 自动匹配与手动 TMDB 匹配共用同一套元数据及文件输出管线，减少两条路径行为不一致的问题。
- 手动纠正流程正确执行 `skip_emby_check`，并在跳过检查时保留明确原因。
- 数据库日志批量写入失败时会恢复原批次等待重试，避免日志静默丢失。
- 历史记录切换状态时，在同一事务内同步关联任务状态和错误信息，避免界面与任务队列显示不一致。

### 前端结构与冲突处理
- 将手动匹配步骤和季集选择器拆为独立共享组件，消除冲突处理弹窗中的重复模板。
- 为各类 `conflict_data` 增加明确的前后端类型，降低冲突数据结构变化导致运行时错误的风险。
- 修正 Emby 冲突中“强制继续”的动作语义，使强制处理不再与普通修改或跳过混淆。
- 引入 Vitest 前端单元测试并纳入 CI，覆盖冲突状态辅助逻辑。

### 代码清理
- 删除 21 个无法从现有前端入口到达的旧文件，以及未被生产代码使用的类型依赖注入、Repository、接口和装饰器空架构。
- 本批重构净减少约 3600 行代码，保留当前用户可达功能并缩小后续维护范围。

### 验证与升级注意
- PR #52 和 #53 的后端测试、前端构建及 Docker 构建均已在 GitHub Actions 通过；未在本机执行构建或测试。
- 保留现有配置、数据库、任务和历史记录，无需清空数据或手动迁移。
- 本版本的多架构镜像、离线镜像包和前端包继续由发布工作流构建。

---

## [2.1.3] - 2026-09-12

本版本集中修复任务与历史记录状态边界、115 网盘访问异常，并优化大队列持久化、运行指标查询和 watcher 生命周期管理。

### 修复
- 修复 115 网盘目录浏览和刮削输出创建客户端时使用已移除参数、导致接口返回 500 的问题；两条路径现在共用与锁定依赖一致的客户端创建逻辑。
- 捕获任务领取后、主刮削流程前的配置、指纹和历史记录异常，确保任务进入明确终态，不再长期停留在运行中。
- 补齐历史记录创建后、任务关联前这一窗口的失败与取消收尾；删除或清空历史前原子检查关联任务，存在等待或运行任务时要求先取消。
- 补齐已删除刮削任务的状态读取，并修正登录历史中的设备名称和 User-Agent 字段映射。

### 性能与可观测性
- 手动任务和刮削任务的旧数据库字段迁移集中到启动阶段，避免每次请求重复执行迁移探测。
- 为历史时间和状态筛选、手动任务筛选、任务关联、文件指纹、路径及反向历史关联增加数据库索引。
- 大批量级联取消改为批量事务更新并同步移除内存队列，只有真正运行中的任务才逐个执行安全取消收尾。
- 刮削日志继续实时推送，但完整 SQLite 日志快照合并写入；任务终态仍强制保存最终日志。
- 运行指标收敛为单连接查询；前端轮询调整为 5 秒，页面隐藏时暂停，恢复后立即刷新，并显示快照时间和查询失败状态。

### Watcher 与后台生命周期
- watcher 启停增加生命周期锁；并发启动保持幂等，部分启动失败会完整回滚，停止失败会保留引用和错误状态以便再次清理。
- watcher 后台任务统一记录未处理异常，实时监控线程的停止等待不再阻塞事件循环，115 目录定位和监控错误日志补充任务及来源上下文。
- 手动任务和刮削任务 worker 停止时取消活动任务、清空内存队列并重置初始化状态；数据库中的未完成任务仍会按既有恢复流程重新入队。

### 验证与升级注意
- PR #47 至 #50 的后端测试、前端构建和 Docker 构建均已在 GitHub Actions 通过；本版本的多架构镜像、离线镜像包和前端包继续由发布工作流构建。
- 保留现有配置、数据库、任务、历史记录及 115 登录状态，无需清空数据或手动迁移；新增字段和索引会在启动阶段自动补齐。
- 升级后可直接重新访问 115 网盘目录；如现有 Cookie 已失效，仍需在设置中重新扫码登录。

---

## [2.1.2] - 2026-09-12

本版本新增真正的任务取消和运行状态可观测能力，便于安全停止任务并判断后台是否仍在处理文件。

### 真正取消任务
- 手动任务与单文件刮削任务支持真实取消，不再只是修改数据库状态。
- 取消手动任务会立即停止继续扫描和投递，并级联取消其尚未完成的刮削子任务。
- 取消运行中任务时会等待文件操作停止或完成安全收尾，避免界面显示已取消后后台仍继续写入。
- 运行中的任务必须先取消再删除，避免任务记录被移除后仍在后台处理文件。

### 运行状态可观测
- 手动任务页新增运行状态面板，展示持久化等待数量、实际运行任务、worker 数、并发上限和最长等待时间。
- 状态面板展示文件 I/O 的实际占用与等待数量，便于识别任务是在排队、执行还是等待磁盘或网络挂载。
- 手动任务列表显示关联刮削子任务的等待、运行和待处理数量，扫描完成后仍可判断后台刮削是否结束。

### 验证与升级注意
- 新增任务取消、通知、运行指标、级联取消和安全文件收尾等回归测试；合并后的 620 项后端测试已在 GitHub Actions 通过。
- 前端检查、后端测试、Docker 构建、容器 HTTP 冒烟检查和 Compose 健康检查均只在 GitHub Actions 执行，不在本机运行。
- 保留现有配置、数据库和历史记录；任务状态与来源索引会在启动时自动补齐，无需清空数据。
- 已完成写入的文件不会因后续取消自动回滚；不可中断的磁盘或网络挂载调用仍需等待系统返回，因此取消收尾可能超过任务超时时间。

---

## [2.1.1] - 2026-09-09

本版本合并升级规划的第二、三批：匹配准确性、TMDB 错误处理，以及任务调度和文件操作优化。

### 匹配与 TMDB 修复（第二批）
- 自动匹配只有一个候选时仍核验标题评分；不再因为搜索仅返回一条结果就自动采用。
- 保留第 0 季特别篇，包括文件名解析和手动纠错任务；缺失的特别篇交由季集核验，不自动改成正片第 1 季。
- 区分 TMDB 无结果、资源不存在、Token 失效、访问拒绝、限流、网络超时和服务异常；请求失败不再伪装成空搜索或季集缺失。
- 网络、超时、429 和可重试服务错误按设置有限重试（最多 3 次），遵守短期 Retry-After，单次请求总耗时仍受任务超时设置限制；长时间限流直接提示稍后重试。
- 后台任务将搜索/API 故障记录为失败，不再误要求手动输入 TMDB ID；增加匹配、特别篇、错误传播及重试边界的云端回归测试。

### 任务与文件操作优化（第三批）
- 历史记录手动处理、重新匹配及失败重试改为持久化入队，与自动刮削共用并发和执行超时限制；接口返回“已加入队列”，不再提前显示“重试成功”。
- 同一记录的并发提交由数据库事务拦截；保留原历史记录、目录、整理方式、云盘定位信息、季集号、覆盖选择和用户日志，服务重启后可恢复排队任务。
- 修改刮削并发配置时保留正在执行的任务计数，避免替换信号量导致短时超出上限。
- 本地目录浏览、手动扫描、监控扫描、视频整理、字幕处理及 NFO 写入移到最多 2 个工作线程的专用文件线程池；等待中的请求不会无限提交到线程池。
- 复制按 1 MiB 分块检查取消，并在发布目标文件前再次检查。超时/取消时等待正在执行的文件操作停止或完成收尾，之后才写入最终状态，避免状态已结束但后台仍继续写文件。
- 同磁盘普通移动保留不复制整文件的快速路径；覆盖和跨磁盘移动先准备完整文件，再发布到目标位置。
- 关闭服务时先停止扫描和投递，再等待刮削及文件操作收尾；监控初始扫描也纳入关闭管理。
- 修复任务超时与服务关闭同时发生时，文件操作尚未收尾就提前记录取消状态的问题。

### 验证与升级注意
- 补充匹配、特别篇、限流重试、持久化队列、并发提交、服务重启和文件取消边界等回归测试；组合代码的 613 项测试已在 GitHub Actions 通过。
- 测试、前端构建、Docker 构建和 Compose 健康检查均只在 GitHub Actions 执行，不在本机运行。
- 保留现有配置、数据库和历史记录；任务字段自动补齐，无需清空数据。
- 已完成写入的文件不会因后续超时自动回滚；不可中断的磁盘或网络挂载调用需要等待系统返回，因此取消收尾可能超过设置的任务超时。重试前应核对日志和目标文件。

---

## [2.1.0] - 2026-09-09

### 修复
- 覆盖视频前先在目标磁盘准备完整的新文件，准备或替换失败时保留原目标和源文件。
- 字幕跟随视频的复制、移动、硬链接或软链接模式，不再先在源目录改名；字幕冲突不自动覆盖。
- 历史记录重试和手动处理从原始任务恢复整理目录、元数据目录、整理模式及高级设置；读取失败时停止操作，避免退回错误的默认模式。
- 元数据目录在刮削开始前校验；最终 NFO 路径也执行媒体目录限制，并以临时文件原子写入。
- Compose 健康检查统一使用镜像已有的 Python，避免缺少 curl 导致健康状态误报。

### 验证与升级注意
- 补充覆盖失败、字幕四种模式、重试参数恢复及元数据路径边界的云端回归测试。
- 测试和镜像构建仅在 GitHub Actions 运行，不在本机执行。
- 覆盖操作需要目标磁盘有足够空间暂存新文件；移动完成但源文件删除失败时保留两份并记录警告。
- 自定义本地元数据目录必须包含在 `MHTI_ALLOWED_MEDIA_ROOTS` 中；不需要清空原有数据库。
- 本版本仅包含升级规划第一批；匹配边界和任务调度优化将在后续版本分别验证。

---

## [2.0.10] - 2026-09-08

### 优化
- 历史记录字段迁移在启动阶段执行，只添加缺失字段，保留旧数据并报告迁移错误。
- 业务数据库访问统一使用连接入口，默认最多 5 个并发连接，每连接 SQLite 缓存预算调整为 4 MiB。
- 连接归还时回滚未提交事务，处理取消与关闭，并修复 UnitOfWork 连接归还。
- 图片以 64 KiB 分块下载，读取过程中执行 20 MiB 限额，完整下载后原子替换目标文件。
- 下载失败、取消或超限时清理临时文件，保留原图片及其访问权限。

### 验证
- 增加迁移、连接并发与取消、流式下载等回归测试。
- GitHub Actions 增加最终 Docker 镜像启动、健康接口及前端页面检查。

### 发布与升级
- 发行页面自动展示本版本中文更新说明；发布前检查更新说明存在且非空。
- 继续使用原有数据目录即可升级，历史记录字段自动补齐，不需要清空数据库或手动迁移。
- 连接并发达到 5 个时后续操作会等待；图片超过 20 MiB 时停止下载并报告失败。

---

## [2.0.8] - 2026-08-13

### 新增
- AI 设置增加“辅助使用”和“强制使用”模式；强制模式在未确认别名时优先交由 AI 识别

### 修复
- 强制 AI 模式在低置信度或识别失败时转为人工确认，禁止常规候选自动选择

---

## [2.0.7] - 2026-07-31

### 变更
- main 分支要求经 PR 和完整 CI 后才能合并，并禁止强推与删除
- 发布前验证来源和版本声明一致性；默认 Compose 固定到发布版本，升级需显式指定版本号

---

## [2.0.6] - 2026-07-30

### 修复
- 从刮削记录详情返回时保留原状态筛选、分页、搜索条件和手动任务来源
- AI 重试待处理改为覆盖全部历史分页，并继续安全跳过不符合条件的记录

---

## [2.0.5] - 2026-07-29

### 修复
- 待处理、已跳过和已删除记录可按 TMDB ID 重新匹配，并在成功后清理旧冲突状态
- watcher 重扫时保留用户的“跳过”或“删除”决定，避免容器更新后重新刮削

---

## [2.0.4] - 2026-07-29

### 修复
- 串行化同一事件循环内的 SQLite WAL 初始化，避免并发创建任务时偶发 `database is locked`
- 在请求 WAL 模式前配置 SQLite busy timeout，允许跨进程锁竞争等待

---

## [2.0.3] - 2026-07-29

### 新增
- 成人 OVA 文件名的多阶段 TMDB 检索与常见发行编号解析
- 基于本地语言、日文集标题和发行月份的谨慎集数匹配
- 从成功人工匹配中学习别名，并幂等回填既有历史

### 修复
- AI 低置信度时仍可扩展只读搜索词，但不能自动修改标题、季集或选择 TMDB
- 同一别名指向不同 TMDB 条目时禁用自动匹配，避免错误复用

---

## [1.0.0] - 2024-XX-XX

### 新增
- 🎬 视频文件名智能解析（支持标准、中文、日文格式）
- 🔍 TMDB 元数据搜索与自动匹配
- 📝 NFO 文件生成（Emby/Jellyfin 兼容）
- 📁 文件整理（复制/移动/硬链接/软链接四种模式）
- 🖼️ 海报、背景图、剧集缩略图自动下载
- 📺 字幕文件自动关联
- 👁️ 文件夹实时监控与自动刮削
- 🔗 Emby 媒体库冲突检测
- 🔐 JWT 认证与多会话管理
- 🌙 亮色/暗色主题切换
- 📊 首页统计概览
- 🐳 Docker 一键部署
- 🌐 WebSocket 实时进度推送

### 技术栈
- 后端：Python 3.11 + FastAPI + aiosqlite
- 前端：Vue 3.5 + TypeScript 5.9 + Naive UI
- 部署：Docker + Caddy

---

## 版本说明

- **[Unreleased]**: 尚未发布的变更
- **新增**: 新功能
- **变更**: 对现有功能的变更
- **弃用**: 即将移除的功能
- **移除**: 已移除的功能
- **修复**: Bug 修复
- **安全**: 安全相关修复
